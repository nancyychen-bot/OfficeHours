# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# production
/build

# env — NEVER commit real secrets
.env
.env*.local
.env.local

# vercel
.vercel

# misc
.DS_Store
*.pem
*.tsbuildinfo

# test
/coverage

# local test artifacts
.playwright-mcp/
/add-event.png
/add-event-page.png

# Local DB snapshots (contain PII — never commit). Anchored to repo root so it
# doesn't also ignore app routes like app/settings/backups or app/api/hub/backups.
/backups/
